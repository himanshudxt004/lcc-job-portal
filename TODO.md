- [ ] Confirm GSAP on `how-it-works.html` by ensuring `.step`, `.journey`, `.approach-card` get animated and also get `.visible` class.
- [x] Update `frontend/js/gsap-animations.js` to add ScrollTrigger animations for:
  - `.steps-grid .step`
  - `.journeys .journey`
  - `.approach-grid .approach-card`
- [x] Ensure those elements get `visible` class when GSAP triggers (so CSS doesn’t hide them).
- [ ] Quick smoke test: open `how-it-works.html` and verify animations on scroll.


